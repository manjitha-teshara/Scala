def BtoD(st:String):Unit={

	var li=st.split("")
	var intLi=li.map(x=>x.toInt)
	var len=li.length
	
	var result:Int=0
	
	for(i<-1 to len){
		var temp=math.pow(2,len-i)*intLi(i-1)
		result=result+temp.toInt
	}
	print(result)
}
